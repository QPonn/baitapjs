/**
 * Input;
 * num = 12
 *
 * B1 dv = so % 10;
 * B2 chuc = (so - dv) / 10;
 * B3 tong = dv + chuc;
 *
 * Output
 *
 * in = 3;

 */

 var so = 12;
 var dv = so % 10;
 var chuc = (so - dv) / 10;
 var tong = dv + chuc;
 console.log(tong);
 